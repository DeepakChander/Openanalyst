# EVM Deep Dive

> Expert in Ethereum Virtual Machine internals - gas optimization, assembly/Yul, opcode-level optimization, and low-level EVM patterns

**Category:** blockchain | **Version:** 1.0

**Tags:** evm, ethereum, solidity, yul, assembly, gas-optimization, opcodes, smart-contracts

---

## Identity

[object Object]

## Patterns

### Storage Packing
Pack multiple variables into single 32-byte slots
**When:** Multiple variables under 32 bytes used together

### Calldata Optimization
Use calldata and tight packing for function parameters
**When:** Functions receive array or struct parameters

### Unchecked Arithmetic
Skip overflow checks when mathematically safe
**When:** Values are bounded or overflow is impossible

### Assembly Storage Access
Direct storage manipulation for complex patterns
**When:** Need precise control over storage slots

### Minimal Proxy (EIP-1167)
Deploy cheap clones that delegatecall to implementation
**When:** Deploying many instances of same contract

### Bitmap Flags
Pack boolean flags into single uint256
**When:** Multiple boolean states per entity


## Anti-Patterns

### Unnecessary SLOAD
Reading same storage variable multiple times
**Instead:** // Bad
function bad() external {
    require(balance > 0);
    uint256 fee = balance / 100;
    balance = balance - fee;
}

// Good
function good() external {
    uint256 _balance = balance; // Single SLOAD
    require(_balance > 0);
    uint256 fee = _balance / 100;
    balance = _balance - fee;
}


### String Error Messages
Using require with string messages
**Instead:** // Bad: ~20k gas overhead
require(balance >= amount, "Insufficient balance");

// Good: Custom error
error InsufficientBalance(uint256 available, uint256 required);
if (balance < amount) revert InsufficientBalance(balance, amount);


### Redundant Zero Checks
Checking for zero when transfer will revert anyway
**Instead:** // Bad: Redundant check
function withdraw(uint256 amount) external {
    require(amount > 0, "Zero amount");  // Remove this
    require(balances[msg.sender] >= amount);
    balances[msg.sender] -= amount;  // Will revert on underflow
}


### Loop Length Recalculation
Reading array length in each loop iteration
**Instead:** // Bad
for (uint i = 0; i < array.length; i++) { }

// Good
uint256 len = array.length;
for (uint i = 0; i < len; ) {
    unchecked { ++i; }
}


### Default Variable Values
Explicitly setting variables to default values
**Instead:** // Bad
uint256 counter = 0;
bool active = false;
address owner = address(0);

// Good - omit initialization
uint256 counter;
bool active;
address owner;


### Memory Over Calldata
Using memory for read-only external parameters
**Instead:** // Bad
function process(bytes memory data) external { }

// Good
function process(bytes calldata data) external { }



## Sharp Edges (Gotchas)

*Real production issues that cause outages and bugs.*

## Collaboration

### When to Hand Off

| Trigger | Delegate To | Context |
|---------|-------------|--------|
| `` | web3-security-audit | Optimized code needs security review |
| `` | solana-development | Non-EVM chain development |
| `` | layer2-scaling | L2-specific optimizations needed |
| `` | nft-systems | NFT-specific optimization patterns |

### Receives Work From

- **blockchain-defi**: 
- **token-launch**: 
- **layer2-scaling**: 
- **web3-security-audit**: 

---

## Get the Full Version

This skill has **automated validations**, **detection patterns**, and **structured handoff triggers** that work with the Spawner orchestrator.

```bash
npx vibeship-spawner-skills install
```

Full skill path: `~/.spawner/skills/blockchain/evm-deep-dive/`

**Includes:**
- `skill.yaml` - Structured skill definition
- `sharp-edges.yaml` - Machine-parseable gotchas with detection patterns
- `validations.yaml` - Automated code checks
- `collaboration.yaml` - Handoff triggers for skill orchestration

---

*Generated by [VibeShip Spawner](https://github.com/vibeforge1111/vibeship-spawner-skills)*
