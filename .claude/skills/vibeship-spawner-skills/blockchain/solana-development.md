# Solana Development

> Expert in Solana blockchain development - Anchor framework, SPL tokens, program development, and high-performance dApp architecture

**Category:** blockchain | **Version:** 1.0

**Tags:** solana, anchor, rust, spl-tokens, web3, blockchain, defi, nft

---

## Identity

[object Object]

## Patterns

### PDA Account Pattern
Using Program Derived Addresses for deterministic account creation
**When:** Need program-owned accounts with predictable addresses

### CPI with Signer Seeds
Cross-program invocations using PDA as signer
**When:** Program needs to sign transactions on behalf of PDA accounts

### Account Validation Pattern
Comprehensive account validation with Anchor constraints
**When:** Any instruction that modifies state

### Zero-Copy Account Pattern
Using zero-copy deserialization for large accounts
**When:** Account data exceeds 10KB or needs frequent partial access

### Event Emission Pattern
Emitting events for off-chain indexing
**When:** State changes need to be tracked by indexers or UIs

### Versioned Account Migration
Supporting account schema upgrades without data loss
**When:** Need to add fields to existing accounts


## Anti-Patterns

### Missing Account Ownership Check
Not verifying the program owns accounts it's modifying
**Instead:** // Always use Anchor's Account<> wrapper or manual ownership check
require!(
    account.owner == program_id,
    ErrorCode::InvalidAccountOwner
);


### Predictable PDA Without User Context
PDAs derived only from static seeds
**Instead:** // Include user-specific data in seeds
seeds = [b"vault", user.key().as_ref(), &nonce.to_le_bytes()]


### Arithmetic Without Overflow Protection
Using basic arithmetic operators on token amounts
**Instead:** // Use checked math or Anchor's require! with explicit bounds
let new_balance = current_balance
    .checked_add(deposit_amount)
    .ok_or(ErrorCode::MathOverflow)?;


### Hardcoded Compute Budget
Assuming fixed compute unit costs
**Instead:** // Let users set compute budget, provide estimates
ComputeBudgetInstruction::set_compute_unit_limit(300_000)
ComputeBudgetInstruction::set_compute_unit_price(1_000)


### Closing Accounts Without Zeroing
Closing accounts but leaving data intact
**Instead:** // Zero account data before closing
let dest_starting_lamports = dest_account_info.lamports();
**dest_account_info.lamports.borrow_mut() = dest_starting_lamports
    .checked_add(source_account_info.lamports())
    .unwrap();
**source_account_info.lamports.borrow_mut() = 0;
source_account_info.data.borrow_mut().fill(0);


### Single Transaction Assumptions
Assuming operations complete in one transaction
**Instead:** // Design multi-instruction flows with intermediate states
pub enum ProcessingState {
    NotStarted,
    Step1Complete,
    Step2Complete,
    Finalized,
}



## Sharp Edges (Gotchas)

*Real production issues that cause outages and bugs.*

## Collaboration

### When to Hand Off

| Trigger | Delegate To | Context |
|---------|-------------|--------|
| `` | web3-security-audit | Solana program ready for security review |
| `` | devops | Program deployment and monitoring setup |
| `` | frontend | Wallet connection and transaction signing UI |
| `` | tokenomics-design | Token model design before implementation |

### Receives Work From

- **tokenomics-design**: 
- **nft-systems**: 
- **cross-chain**: 
- **crypto-trading-bots**: 

---

## Get the Full Version

This skill has **automated validations**, **detection patterns**, and **structured handoff triggers** that work with the Spawner orchestrator.

```bash
npx vibeship-spawner-skills install
```

Full skill path: `~/.spawner/skills/blockchain/solana-development/`

**Includes:**
- `skill.yaml` - Structured skill definition
- `sharp-edges.yaml` - Machine-parseable gotchas with detection patterns
- `validations.yaml` - Automated code checks
- `collaboration.yaml` - Handoff triggers for skill orchestration

---

*Generated by [VibeShip Spawner](https://github.com/vibeforge1111/vibeship-spawner-skills)*
